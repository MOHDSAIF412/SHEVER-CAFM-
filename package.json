{
  "name": "shever-technical-cafm",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix web-admin 2>/dev/null || (tsc -b && vite build) || npm run build",
    "dev": "npm run dev --prefix web-admin || vite",
    "preview": "npm run preview --prefix web-admin || vite preview"
  }
}
